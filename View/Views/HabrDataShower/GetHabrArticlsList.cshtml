@using Model.Types
@model List<ArticleShortInfo>

<div style="display:flex">
    @for (int i = 0; i < Model.Count; i++)
    {
        @await Html.PartialAsync("ArticlShortInfoComponent", Model[i])
    }
</div>